import{AppShell}from'./AppShell';export function SectionPage({active,title,subtitle,children}:{active:string;title:string;subtitle:string;children?:React.ReactNode}){return <AppShell active={active}><section className="empty-page"><h1>{title}</h1><p>{subtitle}</p><article className="card">{children??'Interface preparada para conexão por service, sem dependência direta do backend.'}</article></section></AppShell>}
