import Decimal from 'decimal.js';import{productsService}from'@/services/products.service';import type{PricingSimulationDTO,PricingSimulationResponse}from'@/types/pricing';export const pricingService={async simulate(dto:PricingSimulationDTO):Promise<PricingSimulationResponse>{const product=await productsService.getById(dto.productId);if(!product)throw new Error('Produto não encontrado');const price=new Decimal(dto.salePrice);const contribution=price.minus(product.variableCost);const margin=price.gt(0)?contribution.div(price).mul(100).toDecimalPlaces(1).toNumber():0;return{margin,contribution:contribution.toDecimalPlaces(2).toNumber(),status:margin>=35?'healthy':margin>=25?'warning':'critical'}},async applyPrice(productId:string,salePrice:number){return productsService.update(productId,{currentPrice:salePrice})}};
