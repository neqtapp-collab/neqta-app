import type{SupabaseClient}from'@supabase/supabase-js';import{loadSettingsFromSupabase}from'@/lib/settings';import{costService,priceVariation}from'@/services/costs.service';import{productsService}from'@/services/products.service';import type{DashboardOverviewResponse}from'@/types/dashboard';export interface DashboardService{getOverview(client?:SupabaseClient):Promise<DashboardOverviewResponse>}function safeDiscount(cost:number,price:number,margin:number,minimum:number){if(price<=0)return 0;const fees=Math.max(0,100-cost/price*100-margin);const divisor=1-minimum/100-fees/100;if(divisor<=0)return 0;return Math.max(0,(1-(cost/divisor)/price)*100)}export const dashboardService:DashboardService={async getOverview(client){const[products,costs,settings]=await Promise.all([productsService.list(client),costService.list(client),loadSettingsFromSupabase(client)]);const sellable=products.filter(product=>product.status!=='recipe');const critical=sellable.filter(product=>product.status==='critical');const warning=sellable.filter(product=>product.status==='warning');const priority=[...critical,...warning].sort((a,b)=>a.projectedMargin-b.projectedMargin)[0];const discounts=sellable.map(product=>safeDiscount(product.variableCost,product.currentPrice,product.projectedMargin,settings.financial.minimumMargin));const pressuringCosts=costs.map(cost=>({id:cost.id,name:cost.name,affectedProducts:sellable.filter(product=>cost.type==='packaging'?product.packaging?.some(entry=>entry.id===cost.id):product.components?.some(entry=>entry.id===cost.id)).length,variation:priceVariation(cost.baseUnitCost,cost.previousUnitCost)??0})).filter(cost=>cost.variation>0&&cost.affectedProducts>0).sort((a,b)=>b.variation-a.variation).slice(0,3);return{averageMargin:Number((sellable.length?sellable.reduce((sum,product)=>sum+product.projectedMargin,0)/sellable.length:0).toFixed(1)),targetMargin:settings.financial.targetMargin,reviewCount:critical.length+warning.length,recommendedCount:sellable.filter(product=>product.recommendedPrice>product.currentPrice+.009).length,criticalCount:critical.length,warningCount:warning.length,priorityProduct:priority?{id:priority.id,name:priority.name,margin:priority.projectedMargin,currentPrice:priority.currentPrice,recommendedPrice:priority.recommendedPrice}:undefined,pressuringCosts,promotionOpportunityCount:discounts.filter(discount=>discount>=1).length,maximumSafeDiscount:Math.floor(Math.max(0,...discounts))}}};
